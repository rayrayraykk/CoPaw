# Windows desktop packaging (planned)

CoPaw desktop build for Windows will be added here. Shared packaging logic lives in `scripts/pack/` (e.g. `pyi_project_deps.py` for collecting installed deps). PyInstaller specs and a build script (e.g. `build_exe.sh` or PowerShell) will follow the same pattern as `scripts/pack/macos/`.
