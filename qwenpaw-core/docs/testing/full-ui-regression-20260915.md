# CE3oB2 原前端完整专项回归

延续已批准的原前端交互等价与逐项测试目标。上一轮 `CE3oB2` 已通过九类包检查，但只运行四项相关插件专项。本轮执行当前源码全部 35 个显式项（浏览器场景与原 Python 参考测试，不能统称 35 个浏览器测试），以及原 Console 全量单测。

## Checklist

- [x] 核对 2931 条构建来源、42 个额外脚本、35 项清单和现有九类包哈希。清单包含 28 个浏览器场景与 7 个原 Python 参考项。
- [x] App Server 34 个显式项在当前优化版测试程序中串行全部通过：34 passed、0 failed、0 ignored，354.97 秒，退出 0；未删减断言或只重跑子集。
- [x] CLI 原 Debug 页面优化版显式项 1/1 通过，15.34 秒，退出 0。与 App Server 合计全部 35 个显式项均执行成功。
- [x] 原 Console 全量单测通过：295 个文件、2453 项测试，74.68 秒，退出 0。命令使用原 `test:run` 加 `--maxWorkers=4` 限制资源并发，未改变用例、断言或超时；jsdom 单测不替代真实浏览器。
- [x] 最终汇总及来源/脚本/九类包/源码 Core 哈希核对通过，本轮没有测试失败，没有重试成功子集或改动产品代码。

日志与命令结果：`dist/qa-full-ui-20260915-KrVIvg`。使用 Conda qwenpaw、Node 24、本地协议替身和独立测试目录；浏览器仅使用隔离 headless profile，桌面凭据由测试存储替代。不访问日常数据、系统 Keychain、真实账号；不执行包内 Core、不启动原生 Desktop 或激活 VS Code。Rust 依赖离线锁定。本轮先不改前端、既有测试驱动、产品代码或已交付包；若出现失败，先诊断再制定针对性修复。

插件执行路线仍待用户确认，D3 不变；本轮不因安装包或回归通过而关闭全功能、原生、跨平台及历史不稳定性门禁。

## 覆盖范围与证据

App Server 的 34 项覆盖原备份/恢复/取消、Agent 和 Cron 的隔离与 CRUD/审批/停止、运行中检查点和保存失败恢复、渠道配置/邮件权限、模型配置/聊天/附件/刷新、技能市场、PawApps/插件加载与管理读取、项目创建/克隆/上传/重置等已经实现的场景。其中原 Python 对照共 7 项；加上 CLI Debug 页面，浏览器场景合计 28 项。逐项完整测试名保存于 [verification.json](../../../dist/qa-full-ui-20260915-KrVIvg/verification.json)，不把测试名称数量当作产品功能覆盖率。

原 Console 日志保留 jsdom 的 getComputedStyle/跨文档导航未实现提示，以及错误边界/缺失 Provider 测试的预期错误堆栈。最终 295/2453 汇总全部通过；没有通过隐藏日志、排除用例或修改前端来得到结果。

最终复核于 2026-09-14T20:22:38.870Z 通过：2931 条构建来源、42 个额外脚本、现有九类制品和 source Core 哈希都与本轮前一致。Core 仍为 `ab663c7277c9502e853bd83cb64d76f21bab11c24f541445a5bfd05944e6151b`；[CE3oB2 下载及逐包验收](qa-plugin-manager-packages-20260915.md) 无需因本轮测试重建。

## 不能据此关闭的门禁

- 这是当前仓库全部已标记显式测试的回归，不是对所有旧功能逐条实现完成的证明。插件后端/安装上传/市场、完整外部渠道和其他剩余功能仍开放。
- 本轮没有重新执行普通 Rust 849 项；该基线来源仍匹配。历史 SDK EOF 问题也不在本轮关闭范围。
- Anthropic 等当前浏览器场景成功，不足以证明历史偶发焦点问题根因已经修复；驱动未改。
- 没有原生 Desktop、包内 Core 执行、VS Code 激活或 Windows/Linux/macOS x64 运行验收。
- 没有读取真实 key、改变 D3、commit、push 或发布。总 goal 未完成。
