# 浏览器关闭诊断与整组重跑

对应 [方案](../architecture/browser-shutdown-diagnostics.md) 和主计划 §71。此前 `ZYKOaR` 全量专项有 1 项关闭超时，原失败记录继续保留。本轮不改变原 Console、Rust 产品代码、共享 `closeBrowser` 或 2000 ms 超时；只给新增版本页面驱动增加显式启用的时序记录。

## 已完成的诊断

观察器及共享 DevTools 单测共 16/16 通过；前者新增 3 项，验证事件顺序、协议正文不入日志、退出状态、解绑、快照独立及 64 条记录上限。观察器不发送 CDP 命令或进程信号，原页面与关闭断言仍决定成功/失败。

五次有界诊断运行全部通过。没有延长超时、自动重试或用强杀代替正常退出。每次都观察到 `Browser.close` 响应，随后 socket 结束和主进程 `code=0, signal=null`，最后通过原关闭校验；进程退出之前没有 SIGTERM/SIGKILL。socket 在关闭过程中发出的 error 事件只记录为时序，不被单独认定为根因；原 DevTools 错误处理未改。

| 运行 | 关闭请求至协议响应 | 关闭请求至正常进程退出 |
| --- | ---: | ---: |
| 有限复现 1 | 0.60 ms | 85.96 ms |
| 有限复现 2 | 0.42 ms | 81.77 ms |
| 有限复现 3 | 0.41 ms | 84.03 ms |
| 有限复现 4 | 0.57 ms | 84.58 ms |
| 有限复现 5 | 0.49 ms | 71.92 ms |
| 完整 App Server 专项中的版本页面 | 0.56 ms | 73.02 ms |

上述运行未复现旧超时，**根因仍未确认，没有生产修复**。不将运行通过、CPU 负载猜测或新增日志当作已证明的故障修复。

## 整组门禁

- [x] 当前优化版 App Server 全部 38 个显式项通过，367.65 秒，0 失败、0 ignored。
- [x] 当前优化版 CLI Debug 原页面 1/1 通过，15.34 秒；合计全部 39 项通过、0 失败、0 ignored。
- [x] 2934 构建来源、诊断脚本改动边界、原前端、九包/source Core 哈希和全部终态最终核对通过。
- [ ] 浏览器超时根因、历史偶发问题、完整原功能及原生/跨平台门禁继续开放。

本轮不重跑原 Console 2453 单测；它们在此前同前端源码的完整回归中通过，需与本轮实际执行范围分开。证据目录：`dist/qa-browser-shutdown-20260915-NZqidd`，包括每次 `probes.json` 和独立 `timeline.json`、整组日志/终态。测试仅使用隔离 headless Chrome、测试凭据与本地服务，不执行包内 Core、原生 Desktop 或 VS Code 激活，不读取真实账号/Keychain。

[最终复核](../../../dist/qa-browser-shutdown-20260915-NZqidd/verification.json) 于 `2026-09-14T21:30:26.517Z` 通过。原 45 脚本中仅版本页面驱动增加诊断接入，另外新增观察器及其测试；共享关闭文件、原 Console 和全部产品构建来源保持不变。source Core 仍为 `f3881250…`，九类 `xic9ei` 包未重建或覆盖，不需要因测试观察器重打包。

结论分开记录：本轮整组门禁通过；此前失败证据保留；浏览器退出超时根因仍未解决。没有 commit/push，没有声明全功能/原生/跨平台完成，总 goal 继续开放。
