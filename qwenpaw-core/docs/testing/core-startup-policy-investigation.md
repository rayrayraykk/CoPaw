# 独立 Core 启动：按 PID 关联的策略核查材料

日期：2026-09-09。承接计划 §14.2.24.44 和 [候选验收](workspace-retention-acceptance.md)。当前结论是**需要终端防护侧核查，不是已经确认根因或完成修复**。

## 本轮工具与证据

原生启动探针新增实际子进程 PID、startedAt、finishedAt，保留独立 timeout/外部 signal 判定；单测 3/3 通过，正常用例比较完整返回结构，并将 PID 与子进程自己输出的 PID 对照。仅修改验收工具，没有更改 Core 二进制、打包内容或系统安全策略。

同一 release Core SHA-256：`32e4175cadc808652aeb731d0853925675a278bdc218f0e7f9437e0e0443010f`。

| 运行对象 | PID | 本机时间（UTC+8） | 实际退出 | 同 PID 的终端防护记录 |
| --- | --- | --- | --- | --- |
| 仓库 dist 下的字节相同诊断副本 | 29742 | 13:34:23.121–13:34:34.286 | SIGKILL，11.165 秒，无输出 | 包含非白名单标记 `mac_non_white_proc` |
| target/release/qwenpaw-core 源码构建控制组 | 30133 | 13:37:09.482–13:37:09.806 | 0，324 毫秒，正确版本输出 | 没有上述非白名单标记 |
| 首批 QA tar 解包的 qwenpaw-core | 30275 | 13:38:33.725–13:38:44.905 | SIGKILL，11.180 秒，无输出 | 包含上述非白名单标记 |

最后两项文件名均为 qwenpaw-core，内容哈希一致，不是只比较不同名称的实验副本。三项都没有达到探针超时，失败的 SIGKILL 不是探针发出的。防护记录来自本机运行中的阿里终端防护系统扩展；为避免将无关的主机策略写入项目，此处只保留与核查有关的标记，不复制完整策略标签列表。

原失败归档位置：本轮 `dist/qa-runtime-20260909-3AxXvy/locations.json` 所记录 scratch 下的 `unpacked/core/qwenpaw-core`。仍保留原包、SHA256SUMS、qualification.json、native-execution.json 和新 startup-investigation.json；没有覆盖原失败结果。新的诊断工具版本在原构建之后修改，原 source-inputs.json 仍是原构建时快照，不声称覆盖本轮诊断工具更新。

## 能与不能得出的结论

确认了失败进程和非白名单标签的关联，并有同名、同哈希的成功控制组；先前只从 AMFI 通用签名信息推测签名问题的证据不足。临时签名/新副本/扩展属性对照均没有形成修复。

日志尚未给出“哪个组件向上述 PID 发送 SIGKILL”的直接记录，不能把非白名单标签本身等同于已证实的拦截动作。需要设备的终端防护管理员按上述时间/PID核查策略与处置记录，并在符合设备安全策略时明确授权本机 QA 二进制测试。代理没有修改白名单、停用服务、清理隔离属性、改变系统策略或采用启动重试去越过防护判断。

收到确认后，必须针对独立解包目录的首次启动和真实 SDK 组合重新验收；源码控制组、既有路径后续启动成功、或仅 codesign 完整性通过，都不能替代该验收。全功能目标、Workspace 重新绑定、非默认 Cron、原生窗口及其他平台验收仍未完成，目标保持 active。

## §51 新版本制品的追加观察

不是对旧失败文件进行签名或系统策略修改：按用户构建/测试要求生成的 `dist/qa-runtime-20260909-K8SPWI` 新版 QA，源 Core 为 `dde0c21f48f427e961eaaa5a4b024f66ae10bf2a304acfc9fcb2de518f547361`。首次 tar 探测 PID 39225 于 UTC 13:47:18.578–13:47:29.691 收到 SIGKILL（11.113 秒），无输出且非探测器终止。此 PID 尚未取得新的终端防护侧关联记录，不据旧标签断定新进程的发送方。

同批 DMG/ZIP/平台 VSIX Core 在约 15.8 秒后版本输出成功，随后模型/图片/恢复通过。tar 路径稍后的 WebUI 服务启动也成功。发现旧验收脚本底层探测并行后，为满足用户逐项测试要求改为串行；同字节同路径的后续探测和 SDK 组合通过，时间戳无重叠。该结果不能区分并行影响与系统状态变化，也不能关闭首次启动可靠性；没有向产品加入重试或改变失败文件/防护配置。原失败结果和新串行结果分别保留，详见 [本轮制品记录](qa-usage-packages-20260909.md)。

## §52 邮件版本：串行分发启动再次复现

新输出 `dist/qa-runtime-20260909-jxgzYX`，源 release 哈希 `c9a0b22eb277e143f9b06fcf88197100ae8e64f053323e53f7455b42eb5431e2`。tar、ZIP、DMG、平台 VSIX Core 逐项串行启动均在约 11–12 秒被 SIGKILL，PID 分别为 58768、58779、58883、58992；源 release PID 59033 在 267 毫秒正常退出。探针元数据确认无超时、无主动终止、启动区间无重叠。

针对 2026-09-09 本机 22:52:20–22:53:20 的只读日志查询，四个失败 PID 的 QwenPaw 记录都出现 `mac_non_white_proc`；成功控制组的对应记录无该标记。仍未取得发送 SIGKILL 的直接记录。原失败文件的代码签名完整性检查通过，不改变其字节/路径/属性。该新证据表明串行仍复现，不能仅归因于旧验收脚本并行。完整时间、文件哈希、失败/对照边界见 [邮件版本制品验收](qa-mail-packages-20260909.md)。未反复启动失败文件去等待放行，也未修改终端防护配置；需要设备管理员核查处置来源。
